import { View, Text } from "react-native";
import React from "react";

export default function CommonPopup({ visible, onPressOk }) {
  return (
    <View>
      <Text>CommonPopup</Text>
    </View>
  );
}
